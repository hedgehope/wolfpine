[build-system]
requires = ["setuptools>=64"]
build-backend = "setuptools.build_meta"

[project]
name = "wolfpine"
version = "3.0.0"
description = "A framework for simulating AI accelerators, with Tenstorrent Wormhole and Blackhole backends"
readme = "README.md"
requires-python = ">=3.10"
license = "BSD-3-Clause" 
authors = [ { name = "Nick Brown" } ]
classifiers = ["Programming Language :: Python :: 3"]
dependencies = [
    "numpy",
    "pyyaml",
    "pynng",
    "flatbuffers",
    "pyarrow",
    "pyelftools",
]

version = "1.0"

[project.optional-dependencies]
dev=["ruff", "pytest"]

[tool.setuptools]
packages = ["framework"]

[tool.ruff]
target-version = "py310"
extend-include = ["*.ipynb", "*.pyi"]
exclude = [".venv", "docs/marimo", "framework/bridge/_flatbuf"]

[tool.ruff.lint]
select = ["E", "F", "W", "I", "UP", "PT", "TID251", "INP", "PYI"]
ignore = [
    "E741",   # https://beta.ruff.rs/docs/rules/ambiguous-variable-name/
    "PT006",  # https://beta.ruff.rs/docs/rules/pytest-parametrize-names-wrong-type/
    "PT012",  # https://beta.ruff.rs/docs/rules/pytest-raises-with-multiple-statements/
    "PYI041", # https://docs.astral.sh/ruff/rules/redundant-numeric-union
]

[tool.ruff.lint.pycodestyle]
max-line-length = 130
